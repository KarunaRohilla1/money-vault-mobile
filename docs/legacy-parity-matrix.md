# Money Vault Legacy Parity Matrix

Source of truth: `C:\Users\User\OneDrive\Documents\Projects\money-vault`.

Mobile target: `C:\Users\User\OneDrive\Documents\Projects\money-vault-mobile`.

Status values:

- `MATCHES`: traced legacy behavior exists in backend/mobile and has verification coverage.
- `PARTIAL`: some behavior exists, but workflow/calculation/UI/API coverage is incomplete.
- `MISSING`: no meaningful mobile/API implementation exists.
- `INCORRECT`: implementation conflicts with legacy behavior.
- `BLOCKED`: cannot be completed safely without fixture/test database, design confirmation, or backend endpoint.

| Module | Legacy feature | Legacy source file/function | Mobile implementation | Status | Difference | Required fix | Test |
|---|---|---|---|---|---|---|---|
| Authentication | Select vault by exact vault name and unlock with plaintext PIN hashed through SHA-256 | `app.py` login block; `db.core.hash_pin`; `db.vaults.verify_pin`; `api.auth.login` | `app/(auth)/sign-in.tsx`; `services/api/auth.ts`; `services/api/session.ts`; `api.auth.login` | PARTIAL | Backend login preserves exact name/PIN behavior and leading-zero PINs; mobile submits plaintext PIN to backend and stores only JWT. Manual P0 acceptance is still pending. | Run manual login acceptance on device/emulator and verify no sensitive logs. | Backend auth tests and mobile session tests; manual P0 Authentication pending. |
| Authentication | Logout clears all Streamlit auth and active/shared vault state and clears cache | `app.py.clear_authentication_state` | `stores/authStore.ts`; `services/api/session.ts`; More logout | PARTIAL | Logout now clears JWT, auth contexts, and React Query cache. Onboarding scoped reset is covered by auth-state cleanup but still needs manual verification. | Execute manual logout acceptance from personal and shared active vaults. | Mobile store/session tests; manual P0 Vault Access pending. |
| Vault selection | Personal vault can switch to connected shared vault after entering shared vault PIN | `app.py.render_vault_switcher`; `db.vaults.get_connected_shared_vaults`; `db.vaults.verify_pin` | `features/settings/MorePlaceholder.tsx`; `api.auth` shared activation routes | PARTIAL | Backend validates connected shared vaults through `vault_shares` and requires shared PIN; mobile switches in-session and preserves personal context. Manual P0 acceptance is still pending. | Execute wrong-PIN, correct-PIN, unrelated-shared, and return-to-personal manual scenarios. | Backend shared activation tests and mobile session tests; manual P0 Vault Access pending. |
| Vault selection | Shared vault navigation exposes Dashboard, Shared Expenses, Bills, plus Settings button | `app.py.get_navigation_items`; `app.py.render_sidebar_navigation` | Dynamic tabs and More actions keyed by active vault type | PARTIAL | Shared vault hides personal-only tabs/FAB and exposes Shared Expenses, Bills, Settings under More. Manual navigation acceptance is still pending. | Run manual navigation checks in active Individual and Shared vault sessions. | Mobile `vaultNavigation` tests; manual P0 Vault Access pending. |
| Vault administration | Admin flag controls settings/admin operations | `app.py.activate_vault`; `views.settings.show_settings`; `db.vaults.promote_to_admin/demote_admin/delete_vault` | Settings screen is a placeholder/action hub | PARTIAL | Admin workflows are not represented in mobile. | Add backend endpoints and mobile settings flows only after tracing admin restrictions. | API permission tests and mobile settings tests. |
| Onboarding/setup | Dashboard shows setup prompt until vault has PIN, cycle setting, savings goal, and account | `views.dashboard.show_onboarding`; `db.dashboard.get_onboarding_status`; `db.dashboard.get_dashboard_page_data` | `features/onboarding/*`; `providers/AppBootstrapProvider.tsx` | PARTIAL | Mobile onboarding saves account/cycle/savings goal through backend, but local completion can bypass strict backend setup to avoid trapping existing vaults. | Decide whether mobile must match legacy strict setup exactly or allow local completion override. Prefer backend setup completion once API supports all required state. | API setup-status tests and mobile bootstrap tests. |
| Dashboard | Current financial cycle uses vault financial cycle start day and generated cycle rows | `db.financial_cycles.get_current_cycle`; `db.dashboard.get_dashboard_page_data` | `services/api/dashboard.ts`; `features/dashboard/DashboardScreen.tsx` | PARTIAL | Dashboard uses backend response, but parity tests against legacy fixture are incomplete. | Add fixture-driven comparison between `get_dashboard_page_data` and `/api/dashboard`. | Backend dashboard parity test. |
| Dashboard | Safe to Spend = available salary-account cash - payable settlement - remaining commitments - credit-card due - monthly savings goal, floored at 0 | `db.dashboard.get_dashboard_page_data` | `features/dashboard/adapter.ts`; `DashboardScreen.tsx` | PARTIAL | Calculation lives in backend, but mobile must not fallback to zero on API errors; visual parity not fully accepted. | Keep calculation backend-only and add contract tests for all components of the formula. | Backend API contract test and mobile error-state test. |
| Dashboard | Primary account balance comes from active account ordering by primary, type, name and transaction-adjusted balance | `db.dashboard.get_dashboard_page_data`; `db.accounts.get_primary_account`; `db.accounts.get_accounts_with_balances` | Dashboard API/UI | PARTIAL | Uses backend response; acceptance parity not documented for fixture. | Add fixture asserting primary selection and balance. | Backend dashboard fixture test. |
| Dashboard | Expenses this cycle include actual personal plus shared share rules, not merely raw expense rows | `db.shared_expenses.get_personal_spend_summary`; `db.dashboard.get_dashboard_page_data` | Dashboard API/UI | PARTIAL | Backend likely correct; mobile displays contract output. Fixture coverage needed. | Add test with personal expense and shared expense. | Backend dashboard parity test. |
| Dashboard | Remaining commitments come from planning statuses in active cycle | `db.planning.get_monthly_planning_totals`; `db.dashboard.get_dashboard_page_data` | Dashboard API/UI and Planning API | PARTIAL | Backend exposes value; mobile planning mutations exist but not fully accepted. | Verify paid/cancelled/carried-forward statuses affect dashboard exactly. | Planning/dashboard integration test. |
| Dashboard | Credit-card due is sum of active credit-card accounts with negative balance | `db.accounts.get_total_credit_card_due`; `db.dashboard.get_dashboard_page_data` | Dashboard API/UI | PARTIAL | Backend exposes value; tests needed for credit-card transaction/payment scenarios. | Add fixture with credit-card purchases and payment transfer. | Backend dashboard test. |
| Dashboard | Recent activity excludes zero amounts and Transfer In/Out, newest first, limit 5 | `db.transactions.get_recent_activity_transactions`; SQL in `db.dashboard.get_dashboard_page_data` | Dashboard recent list | PARTIAL | Mobile displays provided rows; add fixture test for transfer exclusion/order. | Add backend response test and mobile adapter test. | Backend dashboard fixture test. |
| Dashboard | Category spending uses actual category spending with shared-expense logic | `db.shared_expenses.get_actual_category_spending` | Dashboard donut/list | PARTIAL | Backend output used; no chart parity for all category cases. | Fixture with regular, shared, and excluded transfer transactions. | Backend dashboard/report test. |
| Needs Attention | Bill/commitment attention cards and upcoming statuses | `views.dashboard.show_dashboard`; `db.shared_bills.get_shared_bills_summary`; planning helpers | Mobile Dashboard | PARTIAL | Current mobile dashboard is simplified relative to design/legacy and may not show all attention conditions. | Trace exact conditions and expose API fields if absent. | Dashboard acceptance checklist. |
| Accounts | Add account with name, account type, opening balance; first active account becomes primary | `views.accounts.add_account_dialog`; `db.accounts.add_account` | `features/accounts/AccountsScreen.tsx`; `/api/accounts` | PARTIAL | Mobile form exists but validation and duplicate-name behavior need legacy parity verification. | Match legacy duplicate handling, primary defaulting, and allowed account types. | API account tests and mobile form tests. |
| Accounts | Edit account preserves or changes primary; cannot duplicate active account names | `views.accounts.edit_account_dialog`; `db.accounts.update_account`; `db.accounts.account_exists` | Accounts screen/API | PARTIAL | API updates account but UI behavior likely simplified. | Add duplicate validation and primary handling tests. | API update tests. |
| Accounts | Delete/archive account; primary reassigned to next active account | `views.accounts.delete_account_dialog`; `db.accounts.archive_account` | Accounts screen/API | PARTIAL | API deletes via archive, but mobile confirmation/transaction warning parity is incomplete. | Add confirmation and blocked/warning behavior matching legacy. | API delete and mobile confirmation tests. |
| Account balances | Balance = opening + Income + Transfer In - Expense - Transfer Out | `db.accounts.get_account_balance`; `db.accounts.get_accounts_with_balances` | Accounts API/UI | PARTIAL | Backend returns balance; mobile displays it. Needs fixture coverage. | Add fixture covering all transaction types. | API accounts test. |
| Add transaction | Supports Expense and Income with account/category/date/amount/notes | `components.transaction_form.transaction_form`; `db.transactions.add_transaction` | `features/transactions/TransactionFormScreen.tsx`; `/api/transactions` | PARTIAL | Basic flow exists. Mobile validation is simpler than legacy form. | Align date, amount, category, account, and note validation/error text. | Mobile form tests and API mutation tests. |
| Add transaction | Shared expense creation with beneficiary shared vault, participants, equal/percentage/fixed allocation | `components.transaction_form.transaction_form`; `db.transaction_shares.*`; `db.transactions.add_transaction` | API supports fields; mobile form does not expose shared allocation | MISSING | User cannot create shared transactions from mobile. | Add mobile shared transaction form using backend allocation fields. | API shared allocation tests and mobile form tests. |
| Edit transaction | Edit reverses/reapplies shared allocation rows and transaction fields | `components.edit_transaction_modal`; `db.transactions.update_transaction` | Mobile edit screen only supports Income/Expense fields | PARTIAL | Shared transaction edit and transfer edit separation are incomplete. | Expose shared allocation edit or route to shared-specific editor; prevent corrupting transfer/shared metadata. | API update tests for shared and normal transactions. |
| Delete transaction | Deleting transfer row deletes paired transfer rows; linked planning statuses reset | `components.delete_transaction_modal`; `db.transactions.delete_transaction` | Mobile delete mutation exists | PARTIAL | Mobile confirmation/error handling needs parity. | Add confirmation, linked-status warning, and transfer-pair expectations. | API delete tests and mobile test. |
| Transaction list | Filters by month, last 3 months, year, category, account, search, date range; sort newest/oldest/amount | `views.transactions.show_transactions`; `db.transactions.get_filtered_transactions` | `TransactionsScreen.tsx`; `services/api/transactions.ts` | PARTIAL | API supports filters; mobile UI currently mostly list/review, not full filter set. | Add filter controls matching legacy options. | Mobile filter tests and API query tests. |
| Categories | System and custom categories; income vs expense; custom category CRUD | `views.categories`; `db.categories.*`; `/api/categories` | `features/categories/CategoriesScreen.tsx` | PARTIAL | CRUD exists, but move-on-delete, system protection, and transaction-count behavior need acceptance. | Match legacy delete fallback/move behavior and system restrictions. | API category tests. |
| Transfers | Transfer creates paired Transfer Out/In rows with same group id and updates balances | `views.transfers`; `db.transfers.add_transfer` | `features/transfers/TransfersScreen.tsx`; `/api/transfers` | PARTIAL | API/mobile exist, but parity for filters, edit/delete, credit-card payment interpretation needs testing. | Add transfer acceptance tests and ensure transfers excluded from spending reports where legacy excludes. | API transfer tests and dashboard/report tests. |
| Credit cards | Credit-card due comes from negative credit-card account balances; payments are transfers | `db.accounts.get_credit_card_due`; `views.transfers`; dashboard | Account/transfer APIs | PARTIAL | Mobile does not make credit-card payment behavior explicit. | Ensure transfer UI supports paying credit card and dashboard due changes. | Credit-card fixture test. |
| Shared expenses | Shared dashboard and shared expenses page aggregate participant spend, paid-by, owe/receive, settlement label | `views.shared_expenses`; `db.shared_expenses.*`; `api.shared` | `features/shared/SharedScreen.tsx` | PARTIAL | Mobile displays summary/settlements/bills but creation/edit of shared expenses incomplete. | Add mobile create/edit shared transaction workflow after API contract review. | Shared expense fixture and mobile tests. |
| Settlements | Mark settlement creates paired transfer-like settlement transactions between participant accounts | `views.dashboard.mark_settlement_dialog`; `db.shared_expenses.settle_outstanding_settlement`; `/api/shared/settlements` | `SharedScreen.tsx` | PARTIAL | Mobile settlement form exists and intentionally exposes participant account selection, but needs exact parity tests. | Add tests for account impacts, partial amount restriction, date, and settlement aggregation. | API settlement test. |
| Bills | Shared bills can be created, edited, cancelled, duplicated, skipped, paid, cycles closed | `views.shared_auxiliary.show_shared_bills`; `db.shared_bills.*`; `/api/shared/bills*` | Mobile can view, mark paid, skip; create/edit/cancel/duplicate/close missing | PARTIAL | Legacy shared bill management is not fully mobile. | Implement missing mobile bill management or mark intentionally backend-only. | API bill tests and mobile flow tests. |
| Planning | Income templates and commitments CRUD with due day, account, amount | `views.planning`; `db.planning.add_*`, `update_*`, `delete_*` | `PlanningScreen.tsx`; `/api/planning` | PARTIAL | Basic CRUD/status exists; legacy UI has richer cycle tabs/activity and close flow. | Match status transitions, close cycle dialog, carry-forward behavior. | Planning API and mobile tests. |
| Planning | Mark income received/cancelled and commitment paid/cancelled/carried-forward; linked transactions handled | `db.planning.save_income_status*`; `db.planning.save_obligation_status*`; `db.core.upsert_linked_transaction` | Planning API/status buttons | PARTIAL | Mobile status actions are simplified and may not collect actual amount/notes/date like legacy. | Add exact forms/fields for status changes. | Planning linked-transaction tests. |
| Financial cycle | Cycle start day supports configured day; cycle generation/close/carry forward preserve dates | `db.financial_cycles.*`; `db.planning.finalize_month` | Backend cycle API via planning/settings; onboarding day 1-31 | PARTIAL | Backend recently supports 1-31; parity with legacy schema constraints and rollover requires tests. | Add tests for days 29-31, month-end clamp, close cycle. | Backend financial-cycle tests. |
| Reports | Reports include date ranges, category breakdown, monthly trend, period summary | `views.reports`; `tests/smoke_reports_calculations.py`; `/api/reports` | `ReportsScreen.tsx`; `services/api/reports.ts` | PARTIAL | Mobile screen is generic/simple; filters and chart behavior incomplete. | Trace report filters and expose UI/API params. | Report fixture tests. |
| Wishlist | Wishlist categories/items summary and CRUD | `views.wishlist`; `db.wishlist.*`; `/api/wishlist` | `WishlistScreen.tsx`; wishlist API | PARTIAL | Core CRUD exists, but full category management/delete fallback/image behavior needs parity. | Align validation, category fallback, account progress, target dates. | API/mobile wishlist tests. |
| Settings | Vault name, PIN, cycle start, savings goal, vault type, shared links, admin operations | `views.settings.show_settings`; `db.vaults.update_vault`; `/api/settings` | More/settings placeholder and onboarding update API | PARTIAL | Current mobile lacks full settings page and PIN/admin/shared link management. | Implement settings module after matrix-approved scope. | API settings tests and mobile settings tests. |
| Currency formatting | Legacy Indian rupee formatting with comma grouping and zero handling | `components.money.format_money` | `lib/format.ts`; settings default INR/en-IN | PARTIAL | Mobile uses Intl; verify exact visible formatting against legacy. | Add formatting examples from legacy expected output. | Unit tests for INR display. |
| Date handling | Legacy stores ISO dates and filters/cycles by database date casts | `db.transactions.get_filtered_transactions`; `db.financial_cycles.*` | Text date inputs and API strings | PARTIAL | Mobile lacks date picker and invalid date validation. | Add date validation/pickers and API validation parity. | Mobile form tests and API validation tests. |
| Permissions | Vault scoping enforced by token vault context and resource ownership checks | `api.dependencies`; `api.resources.*`; legacy session vault id | API dependencies; mobile token auth; shared activation routes | PARTIAL | Active shared vault token is issued only after server-side `vault_shares` validation; unrelated shared vault activation is rejected. Broader resource/admin negative tests remain for later modules. | Add module-specific negative tests as Accounts/Transactions/Shared workflows are completed. | Backend unrelated shared activation test; broader resource tests pending later P0/P1 modules. |
| Error behavior | Legacy shows Streamlit errors; API converts validation/auth/server errors to safe JSON | `api.main`; `api.resources`; views | Mobile ErrorView and mutation messages | PARTIAL | Some mobile mutations still show generic failures; no silent zero should occur. | Surface safe backend messages and no fallback-to-zero for failed financial queries. | Mobile error-state tests. |

## Immediate P0 Workflow Order

1. Authentication and vault access, including shared vault unlock and vault-type navigation.
2. Dashboard contract parity against a fixture database.
3. Accounts and balances, because onboarding and all mutations depend on them.
4. Transactions, including shared allocations and transfer separation.
5. Planning status changes and financial-cycle close behavior.
