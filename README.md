# Money Vault 2.0 Mobile

Production React Native foundation for Money Vault 2.0.

## Commands

- `npm install`
- `npm run typecheck`
- `npm run lint`
- `npm start`

## Environment

Copy `.env.example` to `.env` and provide the existing Supabase URL and anon key.
